{
  "name": "seminar-rkkcs-soft",
  "description": "ライブ抽選アプリ",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run serve\"",
    "setup": "npm install && npm run reset",
    "reset": "rm -f backend/lottery.db && cd backend && npm run seed",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "concurrently": "^8.2.2",
    "eslint": "^9.20.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-plugin-vue": "^9.32.0",
    "prettier": "^3.5.1",
    "typescript-eslint": "^8.24.0"
  }
}
